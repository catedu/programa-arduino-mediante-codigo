"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.Token = undefined;

var _constants = require("./constants.js");

class Token {

  constructor(name, value, line, column, language) {
    // TODO: Update after Flow issue #2646 is fixed.
    // $FlowFixMe
    this.name = name;
    // $FlowFixMe
    this.value = _constants.isIe ? value.replace(/\n/g, "\r") : value;
    // $FlowFixMe
    this.line = line;
    // $FlowFixMe
    this.column = column;
    // $FlowFixMe
    this.language = language;
  }
}
exports.Token = Token;